<%= simple_form_for @rhetoric do |f| %>
  <%= f.input :title %>
  <%= f.input :meigen  %>
  <%= f.input :description %>

  <input type="text" name="speaker_name">
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>